[package]
name = "cardbox"
version = "0.1.0"
# Consumers `require("cardbox")` -> this directory's init.tl
entry = "src/cardbox"

[deps]
htlx = { git = "https://github.com/ynishi/htl-x", tag = "v0.1.0" , patch_dir = "patches/htlx" }
